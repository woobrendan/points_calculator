import { ReqPoints } from "../models/Points/points_models";

const handleManufPoints = async (manufObj: ReqPoints[]) => {
  for (const classifcation in manufObj) {
  }

  try {
  } catch (error) {}
};
